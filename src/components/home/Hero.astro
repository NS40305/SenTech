---
import Container from '../layout/Container.astro';
import Button from '../primitives/Button.astro';
import { localizedPath, type Locale } from '../../i18n/ui';

export interface Props {
  locale: Locale;
  headline: string;
  subhead: string;
  ctaPrimaryLabel: string;
  ctaSecondaryLabel: string;
  heroImage?: string;
  catalogHref?: string;
  stats?: Array<{ value: string; suffix?: string; label: string }>;
}

const {
  locale,
  headline,
  subhead,
  ctaPrimaryLabel,
  ctaSecondaryLabel,
  heroImage = '/images/hero-1.jpg',
  catalogHref,
  stats = [],
} = Astro.props;

const effectiveCatalogHref = catalogHref ?? localizedPath(locale, '/catalog');
---
<section class="hero">
  <div class="hero__media" aria-hidden="true">
    <img
      class="hero__media-img"
      src={heroImage}
      alt=""
      loading="eager"
      decoding="async"
      fetchpriority="high"
      width="768"
      height="600"
    />
    <div class="hero__media-scrim"></div>
  </div>
  <Container class="hero__content">
    <h1 class="hero__headline">{headline}</h1>
    <p class="hero__subhead">{subhead}</p>
    <div class="hero__ctas">
      <Button variant="primary" href={localizedPath(locale, '/contact')}>{ctaPrimaryLabel}</Button>
      <Button variant="secondary" href={effectiveCatalogHref}>{ctaSecondaryLabel}</Button>
    </div>
    {stats.length > 0 && (
      <ul class="hero__stats">
        {stats.map((s) => (
          <li>
            <span class="hero__stat-value">{s.value}{s.suffix ?? ''}</span>
            <span class="hero__stat-label">{s.label}</span>
          </li>
        ))}
      </ul>
    )}
  </Container>
</section>

<style>
  .hero {
    position: relative;
    background: var(--color-primary);
    color: #fff;
    overflow: hidden;
    isolation: isolate;
    display: grid;
    grid-template-areas: 'stack';
    min-height: calc(100svh - var(--header-height));
    min-height: calc(100vh - var(--header-height));
  }
  .hero > * { grid-area: stack; }
  .hero__media {
    position: relative;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }
  .hero__media-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center bottom;
    display: block;
  }
  .hero__media-scrim {
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(180deg,
        rgba(14, 42, 71, 0.88) 0%,
        rgba(14, 42, 71, 0.65) 35%,
        rgba(14, 42, 71, 0.25) 70%,
        rgba(14, 42, 71, 0.05) 100%);
    pointer-events: none;
  }
  .hero__content {
    position: relative;
    z-index: 2;
    align-self: center;
    width: 100%;
    padding-block: var(--space-6);
    max-width: 880px;
  }
  @media (min-width: 768px) {
    .hero__content { padding-block: var(--space-7); }
  }
  .hero__headline {
    font-family: var(--font-display);
    font-size: clamp(28px, 6.5vw, 56px);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: #fff;
    margin-bottom: var(--space-4);
  }
  .hero__subhead {
    font-family: var(--font-body);
    font-size: clamp(15px, 1.6vw, 19px);
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.86);
    max-width: 64ch;
    margin-bottom: var(--space-5);
  }
  .hero__ctas {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-bottom: var(--space-6);
  }
  .hero__ctas :global(.btn) {
    flex: 1 1 auto;
    min-width: 140px;
    justify-content: center;
  }
  @media (min-width: 480px) {
    .hero__ctas :global(.btn) { flex: 0 0 auto; }
  }
  .hero__ctas :global(.btn--secondary) {
    color: #fff;
    border-color: rgba(255, 255, 255, 0.6);
  }
  .hero__ctas :global(.btn--secondary:hover) {
    background: #fff;
    color: var(--color-primary);
    border-color: #fff;
  }
  .hero__stats {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-3);
    padding-top: var(--space-4);
    border-top: 1px solid rgba(255, 255, 255, 0.18);
  }
  .hero__stats li {
    display: flex;
    flex-direction: column;
    gap: 4px;
    min-width: 0;
  }
  .hero__stat-value {
    font-family: var(--font-mono);
    font-size: clamp(16px, 4.5vw, 22px);
    font-weight: 700;
    color: #fff;
    word-break: break-word;
  }
  .hero__stat-label {
    font-size: clamp(10px, 2.5vw, 12px);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: rgba(255, 255, 255, 0.7);
  }
  @media (min-width: 768px) {
    .hero__stats { display: flex; flex-wrap: wrap; gap: var(--space-6); }
  }
  @media (min-width: 768px) {
    .hero__content { padding-block: var(--space-8); }
  }
  @media (min-width: 1280px) {
    .hero__media-img { object-position: right bottom; }
    .hero__media-scrim {
      background:
        linear-gradient(95deg,
          rgba(14, 42, 71, 0.95) 0%,
          rgba(14, 42, 71, 0.78) 30%,
          rgba(14, 42, 71, 0.4) 60%,
          rgba(14, 42, 71, 0.15) 100%),
        linear-gradient(180deg,
          rgba(14, 42, 71, 0.45) 0%,
          rgba(14, 42, 71, 0.1) 60%,
          rgba(14, 42, 71, 0.05) 100%);
    }
  }
</style>
